## Server-only secrets - never exposed to the browser.
LOGPOSE_API_KEY=
LOGPOSE_CREW_KEY=

## Backend URLs (Docker DNS names when in compose, localhost for local dev).
BACKEND_URL=http://logpose-backend:8324
CREW_URL=http://logpose-crew:8325
